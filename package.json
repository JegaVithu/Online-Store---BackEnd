{
  "name": "news-backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "if-env NODE_ENV=production ?? npm run start:prod || npm run start:dev",
    "start:dev": "node_modules/.bin/nodenv app.js",
    "start:prod": "node app.js"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "if-env": "^1.0.4",
    "mysql": "^2.17.1"
  },
  "devDependencies": {
    "dotenv": "^8.0.0",
    "node-env-run": "^3.0.2"
  }
}
